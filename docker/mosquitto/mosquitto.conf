persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log

# MQTT listener
listener 1883
protocol mqtt

# Websockets listener (if needed)
listener 9001
protocol websockets

# Allow anonymous connections for now (change for production)
allow_anonymous true

# Increase verbosity for debugging
log_type all

# Keep default max connections

